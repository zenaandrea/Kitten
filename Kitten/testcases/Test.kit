class Test {
  constructor () {}

  method int loop(int x)
    if (x < 0) then return x
    /*else if (x > 10) then return 1 + this.loop(x - 2)*/
    else {
       this.loop(x - 1);
       return 1 + this.loop(x - 3)
    }

  method int loop2(int x) {
    int i := 0;

    for (i := 0; i < x; i := i + 1)
       i := i + 1;

    return i
  }

  method void main() {
    new Test().loop(10)

    /*for (int i := 0; i < 10; i := i + 1)
       i := i + 1*/

    /*new Test().loop2(10)*/
  }
}
